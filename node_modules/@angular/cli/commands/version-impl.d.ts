import { Command } from '@angular/cli/models/command';
import { Schema as VersionCommandSchema } from '@angular/cli/commands/version';
export declare class VersionCommand extends Command<VersionCommandSchema> {
    static aliases: string[];
    run(): Promise<void>;
    private getVersion;
}
